# Test Credentials — GarlicLedger Pro

This app has **NO authentication** and **NO backend**. It is a single-user, offline-first
local ledger (data stored in the browser/device via AsyncStorage).

- No login required.
- No API keys required.
- No database credentials required.

Preview URL (Expo web, port 3000): opens directly to the Dashboard tab.
